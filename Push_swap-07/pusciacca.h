#ifndef PUSCIACCA_H
# define PUSCIACCA_H

# include "libft/libft.h"
# include <stdio.h>

typedef struct s_arrt
{
	int *arr;
	int count;
} t_arrt;

typedef struct s_stack
{
	int 	size;
	t_stack *next:
} t_stack;

#endif